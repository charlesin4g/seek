# 用户操作手册（版本切换）

## 概述
- 应用支持在线/离线双版本；离线模式下数据保存在本地加密数据库。

## 切换入口
- 个人主页右上角：图标显示当前模式；
- 个人主页「运行模式」卡片：切换开关（Switch）。

## 切换规则
- 切到离线：直接切换；
- 切到在线：先进行数据一致性检查并尝试同步离线变更；

## 常见问题
- 离线无法访问后端数据：属正常现象，请切回在线或等待自动同步；
- 图片加载失败：可能网络不可用，不影响离线票据操作；

## 进阶
- 手动同步：通过后台服务触发（后续提供入口）；
- 查看同步状态：在系统设置或个人主页中显示进度（后续完善）；